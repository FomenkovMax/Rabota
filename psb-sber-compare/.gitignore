__pycache__/
*.pyc
.venv/
venv/
.env

# Папка data закрыта целиком: в ней лежат база, снимки сборов и готовые
# отчёты. Это результат работы, а не исходники — в репозитории им не место,
# и перечислять расширения по одному значит однажды не угадать.
data/*
!data/.gitkeep
!data/sber_example.xlsx
